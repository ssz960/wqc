# Consultant mechanism claim set

Authoritative candidate stream: `../../consultant_mechanism_claims.jsonl`.

Every record is sanitized and pending human review. It is deliberately absent from `manifest.json` until a human reviewer confirms the claim and publication status.
